{% extends 'AppBundle::base.html.twig' %}

{% block title %} Admin {% endblock %}

{% block header %}
    <h1>Змінити пост</h1>
{% endblock header %}

{% block section %}

    {#<table border="2 px">#}
        {#<tr>#}
            {% for post in posts %}
            <li><a href="{{ path('post', {'id': post.id}) }}">{{ post.title }}</a></li>
            <li><a href="{{ path('post_edit', {'id': post.id}) }}">редагувати</a></li>
            <li><a href="{{ path('post_delete', {'id': post.id}) }}">видалити</a></li>
            <hr>
            {% endfor %}
        {#</tr>#}
        {#<tr>#}
            {#<td>Видалити запис</td>#}
            {#<td><a href="{{ path('post_delete', {'id': 'x'}) }}"> delete </a> </td>#}

        {#</tr>#}
    </table>
{% endblock section %}
